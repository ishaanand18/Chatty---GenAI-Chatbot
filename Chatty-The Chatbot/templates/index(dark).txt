<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Chatty - Your Virtual Chatting Buddy</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      font-family: "Segoe UI", sans-serif;
      background: linear-gradient(135deg, #0a0a0f, #1a1a24, #2a223a, #3a2a4f);
      color: #eaeaea;
    }

    /* Header */
    .header {
      background: linear-gradient(90deg, #1a1a24, #2d1b4f, #4b0082);
      color: #e0c3fc;
      padding: 15px 20px;
      font-size: 22px;
      font-weight: bold;
      display: flex;
      align-items: center;
      box-shadow: 0 3px 12px rgba(0,0,0,0.9);
      border-bottom: 1px solid #2d2d3a;
      text-shadow: 0 0 8px rgba(224,195,252,0.8);
    }
    .header i {
      margin-right: 12px;
      font-size: 26px;
      color: #d8b4fe;
      text-shadow: 0 0 15px rgba(167,139,250,0.8);
    }

    /* Chat container */
    .chat-container {
      flex: 1;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    /* Chat box */
    .chat-box {
      width: 100%;
      max-width: 800px;
      height: 85vh;
      background: rgba(18,18,26,0.95);
      border-radius: 18px;
      display: flex;
      flex-direction: column;
      box-shadow: 0 12px 35px rgba(0,0,0,0.9);
      overflow: hidden;
      border: 1px solid #3a2f55;
      backdrop-filter: blur(10px);
    }

    /* Messages area */
    .messages {
      flex: 1;
      padding: 20px;
      overflow-y: auto;
      background: linear-gradient(180deg, #0c0c13, #151520, #1e1b2f);
    }

    .message {
      display: flex;
      align-items: flex-end;
      margin-bottom: 16px;
    }

    /* Avatar icon style */
    .message .icon {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
      margin: 0 10px;
      flex-shrink: 0;
      background: radial-gradient(circle at top left, #a78bfa, #4b0082);
      box-shadow: 0 0 15px rgba(167,139,250,0.6);
      color: #fff;
    }

    .user .icon {
      background: radial-gradient(circle at top left, #6366f1, #1e1b4b);
      box-shadow: 0 0 12px rgba(99,102,241,0.6);
    }

    /* Message bubbles */
    .bubble {
      max-width: 70%;
      padding: 12px 16px;
      border-radius: 18px;
      font-size: 15px;
      line-height: 1.5;
      box-shadow: 0 3px 10px rgba(0,0,0,0.6);
    }

    .assistant .bubble {
      background: linear-gradient(145deg, #201f31, #2e2b4d);
      color: #e2d8f8;
      border-top-left-radius: 6px;
    }

    .user {
      flex-direction: row-reverse;
    }

    .user .bubble {
      background: linear-gradient(145deg, #4f46e5, #6d28d9);
      color: #f5f5ff;
      border-top-right-radius: 6px;
    }

    /* Typing indicator */
    .typing {
      display: flex;
      align-items: center;
      gap: 6px;
      color: #a78bfa;
      font-size: 14px;
    }
    .dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background: #a78bfa;
      animation: blink 1.5s infinite;
    }
    .dot:nth-child(2) { animation-delay: 0.2s; }
    .dot:nth-child(3) { animation-delay: 0.4s; }

    @keyframes blink {
      0%, 80%, 100% { opacity: 0.3; }
      40% { opacity: 1; }
    }

    /* Input area */
    .input-area {
      display: flex;
      border-top: 1px solid #2d2d3a;
      background: #151520;
    }
    .input-area textarea {
      flex: 1;
      padding: 14px;
      border: none;
      outline: none;
      font-size: 15px;
      background: #1c1c2a;
      color: #e2d8f8;
      resize: none;
      line-height: 1.5;
      max-height: 120px;
    }
    .input-area textarea::placeholder {
      color: #8b8ba7;
    }
    .input-area button {
      background: linear-gradient(135deg, #a78bfa, #7c3aed);
      color: #12121a;
      border: none;
      padding: 0 25px;
      font-size: 15px;
      cursor: pointer;
      transition: 0.3s;
      font-weight: bold;
      text-shadow: 0 0 6px rgba(255,255,255,0.3);
    }
    .input-area button:hover {
      background: linear-gradient(135deg, #c084fc, #8b5cf6);
      box-shadow: 0 0 12px rgba(168,85,247,0.7);
    }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <i class="fas fa-robot"></i> Chatty - Your Virtual Chatting Buddy
  </div>

  <!-- Chat container -->
  <div class="chat-container">
    <div class="chat-box">
      <div class="messages" id="chatBox">
        <div class="message assistant">
          <div class="icon"><i class="fas fa-robot"></i></div>
          <div class="bubble">Hello! Chatty here, ready to chat anytime with you. So, what's new today?</div>
          
        </div>
      </div>
      <div class="input-area">
        <textarea id="userInput" placeholder="Type your message..."></textarea>
        <button onclick="sendMessage()">Send</button>
      </div>
    </div>
  </div>

  <script>
    const input = document.getElementById("userInput");

    // Handle Enter vs Shift+Enter
    input.addEventListener("keydown", function(event) {
      if (event.key === "Enter") {
        if (event.shiftKey) {
          // allow new line
          return;
        } else {
          // send on Enter
          event.preventDefault();
          sendMessage();
        }
      }
    });

    async function sendMessage() {
      const message = input.value.trim();
      if (!message) return;

      addMessage(message, "user");
      input.value = "";

      // Add typing indicator
      const typingDiv = document.createElement("div");
      typingDiv.className = "message assistant typing-indicator";
      typingDiv.innerHTML = `
        <div class="icon"><i class="fas fa-robot"></i></div>
        <div class="bubble typing">
          <div class="dot"></div>
          <div class="dot"></div>
          <div class="dot"></div>
        </div>`;
      document.getElementById("chatBox").appendChild(typingDiv);
      document.getElementById("chatBox").scrollTop = document.getElementById("chatBox").scrollHeight;

      // Fetch response
      const response = await fetch("/chat", {
        method: "POST",
        headers: {"Content-Type": "application/json"},
        body: JSON.stringify({message})
      });
      const data = await response.json();

      // Remove typing indicator
      typingDiv.remove();

      // Add assistant reply
      addMessage(data.reply, "assistant");
    }

    function addMessage(text, role) {
      const chatBox = document.getElementById("chatBox");
      const div = document.createElement("div");
      div.className = "message " + role;

      const icon = document.createElement("div");
      icon.className = "icon";
      icon.innerHTML = role === "user" ? '<i class="fas fa-user"></i>' : '<i class="fas fa-robot"></i>';

      const bubble = document.createElement("div");
      bubble.className = "bubble";
      bubble.textContent = text;

      div.appendChild(icon);
      div.appendChild(bubble);

      chatBox.appendChild(div);
      chatBox.scrollTop = chatBox.scrollHeight;
    }
  </script>
</body>
</html>
